## Ownerships :

> In rust, a memory reference can have only one owner or variable, if we point it on other variable the ownership will completely transfer to that other variable. Previous variable can't access that reference anymore. 


