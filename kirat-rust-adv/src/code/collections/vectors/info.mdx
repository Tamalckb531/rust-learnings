# Vector as a collection

>  Rust has the concept of collection types which are used to store multiple values in a single data structure. One of the most commonly used collection types is the vector, which is a resizable array that can hold values of the same type.